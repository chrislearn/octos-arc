Requirement {node_id}: {description}

Public acceptance example (implement the full requirement):
{spec}
{files_intro} frontend/src/index.html is the shell; backend/server.js serves ../frontend/dist on process.env.PORT||{port} and dispatches API routes from backend/routes/<area>.js.{ports} Keep server.js a small, stable entry point and put shared persistence in backend modules. Preserve installed architecture, exact dependency pins and lockfile. Complex fresh apps use React/Vite/Radix/React Router; use the existing build.mjs and main.jsx/App.jsx. Simple or existing apps keep their architecture. Add only task-required optional dependencies. JSX/TSX must be compiled, not copied. Browser scripts, styles, fonts and media must be local: no CDN URLs or remote imports. npm downloads during install/build are allowed. Use Radix overlays and controlled React state, not custom DOM widgets or delegated global handlers. Use native accessible inputs when enough; Radix Checkbox uses onCheckedChange, DropdownMenu.Item uses onSelect, Dialog requires Portal/Overlay/Title/Description. Keep one state owner per draft and ignore stale asynchronous responses.
For persistent data, initialize required records only for a new store or an explicit migration. Later startups must preserve user edits, deletions and archive state; a missing record does not mean the store is new. Reset data only when the requirements explicitly demand it.
Rules: implement the requirement for general valid inputs and preserve existing behavior. Register literal API routes before overlapping :parameter routes of the same method. Use required labels and accessible controls, with unique IDs and correct label associations. A click does not await its async handler: mount usable editor/dialog controls before the first await; isolate background only for modal overlays. Await persistence and list refresh (or update optimistically), and retain edits on failure. Derive Save/Cancel/autosave transitions from requirements; cancelling a draft must not commit it. Derive storage, rendering, styling and validation from the task; do not hardcode test outputs. Return only requested file blocks. {size_rule}
