# v11.1 后续改进方案与复审

日期：2026-09-26。状态：**已完成一轮有界实施与代码复审；未做付费整题复测**。本文第 1～6 节保留实施前的证据和方案；第 7 节记录实际改动、验证与剩余边界。此前停止后台任务时没有修改运行中的生成产物或原始证据。

## 1. 证据边界与目标

修复后本地顺序复测目录为 `arc/diagnostics/local-v11.1-repaired-20260926-143827/`。收到停止指令后，driver 收到 SIGTERM，`status.json` 记录 `interrupted`，GitHub 容器退出码 143；Sheet 尚未启动。定时跟进 `v11-1` 已暂停。GitHub 停在团队成员节点的修复过程中，不能把中断状态当作该节点最终失败，也不能推断 Sheet 的修复后效果。本次共完成 109 次 proxy 请求，唯一 proxy 层 usage 约 267 万 token；它是成本观察，不是正式分数。此前修复的 1641 项回归全过，说明已覆盖的旧分支没有回归，不能替代本轮真实生成诊断。内部派生测试成绩始终不能称为平台官方 100 场景成绩。

证据以本轮 `report.md`、`github/evidence/audit-*.json`、原始 `github/evidence/exchanges/`、生成 spec 与代码快照为准。停止前新增的 `proxy-00107`～`00109` 有一条尤其明确的反例：两个 `Members` 机械 reach 测试中，Scenario 2 没有按权威 GIVEN 登录；Scenario 1 的有效导航至少需要四次点击，而 helper 默认只搜索三层。模型为让 reach 通过，在 `github/workspace/template/frontend/src/pages/OrganizationPage.jsx` 增加 `Members` 链接，链接本身带 `onClick={e => e.preventDefault()}`，点击不导航；还把组织、仓库、团队加载放进同一个 `Promise.all`，新增团队接口失败时会使整页进入错误状态，这是可从代码直接推出的耦合风险。**新增链接存在且不可点击是已确认源码事实；新接口失败是否实际影响页面、这些改动是否进入最终产物及测试结果，因为任务中断，尚未验证。** 这证明“错误测试驱动改应用界面”的风险已从审核意见演变成实际写入，方案必须优先切断这条链。

目标按顺序是：判定证据可信、只让有效测试驱动修复、保住已验证行为、降低无效模型调用，最后才考虑调大预算。以下把已确认缺陷与建议机制分开；参数是待验证候选值，不冒称最优。

## 2. P0：先建立测试可信度边界

### 2.1 生成测试的需求合同与分支

在生成 spec 前，为每个原始 scenario 建一个小型、可追溯的合同账本：GIVEN 的身份/初始记录和状态、WHEN 的动作序列、THEN 的必要结果，以及条件分支、负向结果、跨会话/刷新/持久化要求。每项记录需求原文位置、稳定 ID 和来源，不靠 UI 文案出现次数计覆盖。最初仅覆盖当前 DSL 能表达的状态转换、权限、写入/不写入、重新认证和导航；无法表达的结果标 `unrepresented`，不伪造 pass。正式平台与 public 测试只读保留；本机制只管理内部 derived 测试。

校验时按**分支自身**的前置和期望判断，而非把母场景的退出后 `Sign in` 期望套到 `Cancel` 分支。本轮合法取消退出被拒、恢复测试重试删掉新旧密码重新认证与未知邮箱分支，却仍显示字面结果 2/2、missing 0。新版本 spec 与上一版本、权威合同逐项对照：必须保留每个有依据的结果，或者将其显式标为 `disputed/unrepresented` 并维持覆盖缺口；不能靠删断言获得完整覆盖。原 oracle 若本来有错，允许受控纠正，不能机械冻结错误文本。注册成功须证明新账号可使用；恢复须使用要求的新注册账号，并测试语法有效但不存在的邮箱。失败不写、刷新后状态和角色权限分别计量。单纯按钮可见、URL 可达或文案回显不计业务行为通过。

编译/校验增加**反向样例**：正确的取消后会话保留应通过；删掉重认证或负向不写断言应留覆盖缺口；无需求来源的精确文案和输入框回显仍应拒绝。避免为解决误拒绝而放宽所有 identity/exit 断言。需要新增合同提取和版本比较代码，仅改提示词无法阻止模型删 oracle。

### 2.2 测试争议、隔离与修复的顺序

当前 `arc/main.py` 的审核提示开头要求 `verdict/evidence/scenarios`，后段才要求 `reason_code/requirement_quote/test_quote`，而 `arc/test_policy.py` 必须收到后三者才能做 grounded 裁决。先统一为一个前置 JSON schema，字段、枚举、例子与校验完全一致；若缺字段，仅对**同一证据**做一次格式纠错请求，不重新解释需求。审查输入以目标失败为主，完整给出该测试的动作、运行时 DOM/请求/响应、相关已通过用例及其源码/fixture/hash；同节点其他失败放在摘要区。计划输入不等于实际填入值，搜索耗尽不等于页面缺失，HTTP 状态和异常要逐层定位。

执行状态分 `valid_failed`、`harness_fault`、`suspected_spec_error`、`invalid_quarantined`、`unresolved`、`blocked`、`passed`。在证据不足或审核 schema 失效时先把争议测试标为 unresolved，不再把它作为改应用的目标；有效失败仍继续修。确定错误需需求引用 + 测试原句 + 可复现冲突；语义争议由不带首审结论的第二次只读审核，意见不一致保持 unresolved。同一个模型两次一致也不是独立真理，机械可验证前置错误优先靠确定性规则和复现裁决。只隔离准确的 derived case，原 spec 与判定账本保留，版本/hash 变化使裁决失效。禁止改测试时仍允许隔离，但不改写 public/official suite。

**隔离后不能晋升功能。** 报告原始用例数、启用/通过/失败/隔离/unresolved/blocked、行为结果覆盖及未验证合同；有效测试全过但场景缺失，显示 `implemented_unverified`。不能把隔离后的 2/2、入口 2/2、team 创建入口 2/2 或安全页的部分行为 4/4 当成完整需求通过。避免对错误测试做“改应用以过测”：在任何源代码修复前，尤其机械 reach/entry 失败、无效前置、helper 超时和单一错误断言失败，先作可信度分流。停止前 `Members` 的不可点击快捷链接应成为专门回归：不得因三层 reach 失败新增无需求依据的控件；若新入口确有独立产品依据，也必须可用且验证真实目的地，并确认相关新增请求失败不破坏原有组织概览。

### 2.3 测试 helper 的确定性

`clickNamed/openNamed` 等 SPA 导航等待目标视图稳定及所需控件，而不只等 `domcontentloaded`；`fillField` 以所属 form、精确 label/role 定位，填后核对当前控件的值和提交请求，不使用可匹配旧 `Username or email` 的非锚定 `Username` 搜索。本轮复制页面的隔离重放，原 helper 在不同运行中把注册 Username 丢掉，精确等待对照可正常填写。这是确定的 helper 问题，不应让模型反复修改后端校验来弥补。

`reach` 改为优先使用需求明确导航路径、已通过的路径缓存及局部菜单探索；全图搜索只作诊断，报告 `search_budget_exhausted/depth_exhausted/target_absent/route_error`，不给应用错误的笼统结论。团队页四层合法导航应逐步走路径，不能为 depth3 测试扁平化产品结构。负例也要保留：无入口或权限不足应仍能发现。不要仅把 40 秒/3 层上限调大，那会放大成本且可能继续被图形变化误导。helper 版本变化使相关测量与测试裁决失效；先修 helper 再重测，不能批量 quarantine 业务用例。

## 3. P0：状态、权限和应用写入一致性

### 3.1 单一调度入口与版本化测量

把波次、顺序、`whole_app_experiment` 中的 `node_cycle`/repair 都接入同一 `admit(node, capability_evidence)`：前置失败时暂缓依赖该能力的执行、保留独立节点；若另有实测 seed 身份能力可绕过特定失败前置，需显式记录例外证据，不能只按树关系一刀切。前置恢复、源码回滚或测试/fixture/helper 改版后重新判定。当前修复路径绕过 `pending_dependencies`，注册仍失败时登录/组织/团队继续，是已确认漏洞。

每次测试结果绑定源码树、测试与 helper、fixture/reset epoch、配置、服务进程和构建产物指纹。`rollback_failed_extension` 在恢复源码后必须重测受影响合同，并原子更新内部 verdict、`node_states.json`、runner event 与 coverage；回滚只说明回到了旧源码，不能自动恢复旧 `PASSED`。本轮安全页重新实测 7/7，但外部 node state 仍 FAILED，说明现有投影不同步。若新节点再次改共享文件，既有测量应标 stale，不能引用旧 7/7 保证当前正确。

### 3.2 应用候选与回滚判定

应用编辑先进入隔离候选，确认工具写入与真实 diff/hash，构建、最小合同 smoke 和受影响已通过行为后提交。已有整答 FILE 拒收原子性保留，但新方案还要覆盖 tool edit、共享文件重写与“写入未变却计进度”。候选 diff 和测量必须绑定；回滚仅撤候选应用源码及其测量，不撤合法测试账本。对复合修复允许拆出可独立验证的子改动，但不得因为静态判断正确就绕过运行失败。

回滚使用分层结果：真实行为退化优先于机械发现噪声；同分但直接接口合同从 500 修成成功的局部改善须再做有针对性的复测，不机械回滚，也不无条件保留。曾有 `accounts.update→patch` 真正修复不存在的方法却因总分持平被撤销；另一次共享 AppLayout 删除 `Settings → Password and authentication` 后安全行为 7/7→4/7，确实需要回滚。这两类不能合并为“回滚总是错误”。共享布局改动前后保持已证明导航合同，并执行旧流程的显式行为；保护对象是可用路径与语义，不强制永久保留某个组件结构。

### 3.3 工具和数据合同

模型可见工具列表必须与实际执行器的 allowlist 一致，并在**执行点**按本轮授权与参数校验；撤去 `read_file` 后还执行成功，是已确认协议边界缺陷。若执行器在不可维护内核里，先定位边界，选择可验证的 adapter 代理拒绝或升级内核接口；提示“禁止再读”只能辅助，不能当权限修复。拒绝后给标准工具错误，不隐式执行、不吞掉真实 edit 结果。对每次 read 记录路径、范围、hash/截断和是否同一轮已返回；同一源码跨轮重读可能必要，不能简单封禁。

为存储集合指定单一 owner、共享 API 与初始化/迁移策略；同名集合不同 `initial:[]` 静态标冲突并用 fresh-store、existing-store 两种集成测量定因，不以重新 seed 覆盖用户数据。本轮空盘隔离重放已见登录成功而私有组织缺失、公开仓库详情 404；这不是只读静态猜测。为集合方法提供最小类型/契约检查，拦住不存在的 `update`；再通过真实路由请求确认 400 字段错误与 500 方法异常，不能靠“源代码看似正确”宣布通过。源码脱敏保留变量名、字段名、调用结构，只隐藏敏感**值**；未知敏感文本仍由安全过滤处理，不建议用编码绕过。

React Router 的静态 route 检查须合成 pathless、index、nested relative 与动态段再判缺失。不能简单压掉全部 ROUTE_LINK 告警：解析不能确定时输出 `unknown` 与需验证 URL，真实缺路由仍由运行 smoke 报错。本轮相对路由与 `/` index 多次误报，触发付费 focused repair、模型只读后零修改；先消除确定误报，再决定是否发模型调用。

## 4. P1：提示词、预算和反馈

提示词首屏放 active requirement ID、权威结果清单、当前候选 diff、可用工具、与本失败有关的最小证据。审核模型必须区分“计划参数”“页面实际值”“网络实际请求”“已有通过的相关用例”，且不得把 source review 写成实测完成。输出 `observed / inferred / unverified / next_check`，每个断言指向证据；这可减少无证据结论，但**无法替代**上述测试/工具/状态硬门禁。设计输出的结构 schema 已能纠正 `modules.owns` 类型；软 150 行/12k 字符限制仍被忽略，应按 schema 控制字段长度或分段局部修复，并由代码拒收超出硬限制的无关展开，不能裁剪必要需求合同。

预算区分 provider 请求、工具调用、内核 checkpoint、每轮修复、整题时间/token。现有单次工具修复 8→12 只在真实源码变化时扩容，在一个样本中工作；本轮另一修复先结构化 8 请求零变更，再 fallback 12 请求，同一问题共 20 次，表明**轮级**上限缺失。建议先设共享轮级基础 8～12、证据化追加上限 16 的实验档，同时保留时间/token 和最终验证预留；checkpoint 也计实际 provider 请求。只有内容 hash 变化、正确的因果定位或新可验证结果才考虑扩容，`write` 但 `changed=false` 不算进展。预算值先在已记录失败输入与有限任务样本校准；不能现在笼统调大，也不能把缺席的 Sheet 数据当作预算依据。模型仍可进行必要的分段读取；给它已读文件 hash/range/截断清单，减少同轮无变化重复读取。全任务输入窗口另依 `_refactor.md` 的输入/输出解耦方案评估，本轮证据主要支持修复轮预算边界，不支持无条件扩大 prompt。

反馈应先报告具体合同和失败层：例如“退出 confirm/cancel 有行为通过，但 S7/dialog 合同未覆盖”，而非“只有入口测试”；“搜索达到深度/时间上限”而非“页面不存在”；“HTTP 400 的实际 body 字段不匹配”与“500 的 collection method 缺失”分开。`coverage` 同时展示运行结果和合同覆盖，避免 model 为提高通过计数去加无效快捷链接或删 oracle。

## 5. 实施顺序、验收和停损

| 阶段 | 修改范围 | 必须通过的验收 | 停损条件 |
| --- | --- | --- | --- |
| A：测试可信度 | 合同账本、分支校验、审核 schema、隔离账本、helper | 回放取消退出、恢复 oracle 删除、注册丢字段、未登录/四层 Members；错误测试不再驱动应用编辑，有效真实失败仍会失败，隔离不晋升功能 | 出现有效失败被静默 skip、需求结果被删却报完整、public/official 用例受影响 |
| B：编排一致性 | admission、版本化结果、候选提交/回滚、执行器 allowlist | 前置失败阻断依赖而独立节点继续；回滚后 state/event/verdict 一致；撤下工具返回拒绝且未执行；同分真实 API 修复获得针对性复测 | 旧 pass 被无条件复活、工具执行越权、应用变更与测量指纹不一致 |
| C：应用合同 | collection owner/方法、route 解析、共享 UI 差分 | 空盘和已有数据均维持身份/权限/仓库详情；相对/index 路由不误报，真实缺路由仍检出；Settings 路径在共享 UI 改动后仍可走 | 数据被重 seed 覆盖、静态检查大规模误拦、共享行为真实退化 |
| D：效率 | prompt/反馈/轮级预算 | 记录每轮请求/token/内容变更/实测增益；零变更 fallback 不能再次完整花 12 请求；关键结果不因精简输入丢失 | 调用成本升高而无行为覆盖改善，或更早耗尽导致验证减少 |

每阶段先用保存的真实失败输入做**无付费或有限独立重放**，加入正反向回归，再跑原有完整测试、构建和针对性浏览器/API 集成；1641 项旧回归是基线，不是新机制的充分验收。A/B 若需 provider 支出，明确单次重放不是完整任务复测。全部确定性门禁稳定后，才另行决定是否做 GitHub→Sheet 两个完整顺序生成对照；本次停止指令下不自动重启。对照需冻结版本、需求、模型和预算，报告真实耗时、唯一层 usage、测试隔离与未覆盖、可启动性和实际行为，不以内部百分比替代官方得分。

## 6. 方案复审：能解决什么，副作用如何控制

| 提议 | 解决依据 | 主要副作用/可能的新错 | 收敛办法及审查结论 |
| --- | --- | --- | --- |
| 合同账本 + 禁止删 oracle | 取消分支误拒与恢复结果被删后仍“完整” | IR/提取错误可能挡住合法测试；生成更慢 | 先只登记有原文依据且可表示的结果；无法表达标缺口，允许证据化纠错；正反回归。**能解决已见字面覆盖虚高，不能保证所有语义都被提取。** |
| 争议隔离前移 | Members 与注册/恢复坏前置诱导应用修复 | 误隔离真实应用失败，造成假绿 | 精确 case/hash、需求引用、二审/机械复现、unresolved 不驱动应用但不晋升；有效失败继续。**误判风险仍在，账本与状态防止隐匿。** |
| helper 精确导航/填值 | 旧 DOM 误填、广搜超时 | 等待过严可能误判异步 UI；路径缓存可能过期 | 控件语义 ready + 提交值核对，按源码/route 状态失效缓存；保持探索兜底和负向测试。**比固定 sleep/加深 BFS 更有针对性。** |
| admission + 版本化结果 | whole-app 绕过依赖、回滚状态过期 | 过保守阻断可独立节点；重测增加时间 | 能力级实测例外、独立节点继续、受影响闭包重测而非全套重跑。**须先补事件一致性，再用状态作硬门禁。** |
| 候选提交与分层回滚 | 真 patch 因总分持平撤销；共享菜单真实退化 | 更多临时快照与磁盘/时间；局部合同好却整体坏时决策复杂 | 最小候选、按实际受影响行为测，真实回归优先；没有充分测量保持未验证，不擅自提交。**可减少误回滚，但不能免除人工/后续深测。** |
| owner/route 静态合同 | fresh-store 丢数据、相对路由误报 | 过拟合 React Router/特定后端 API，误拦其他栈 | 仅在识别到的框架用专门规则，其他情况 unknown + runtime；fresh/existing 数据分测。**不要用字符串正则替代运行测试。** |
| 工具执行 allowlist | 未提供的 `read_file` 实际执行 | 阻断模型必要读取，可能降低修复率 | 每轮允许工具清单清晰，必要读阶段显式开放，切换编辑阶段给标准拒绝与补救入口；回归撤权执行。**必须落到执行器，不是 prompt。** |
| 轮级预算 + 短反馈 | 无进展 8+12、误报 route 多次调用 | 太紧会让复杂修复早停；精简会丢关键证据 | 预算保留 8→12 单轮进展策略，异常 16 实验档和人工可读停止原因；目标证据不截断，超限转 unresolved。**先测效益再改默认。** |

复审后的优先级调整：先修错误测试与 helper、阻断测试诱导的无效应用编辑，再修状态/回滚和工具边界；不要先扩大模型预算。提示词可改善输出约束和证据表达，但已确认的分支校验、缺失语义覆盖、SPA 时序、执行器权限、状态投影和 route 误报都需要代码改动。即使这些措施通过全部本地回归，也只能说明指定失败模式受到控制；Sheet 尚无本轮实测，其他未覆盖场景仍需单独验证。

## 7. 本轮实施、代码复审与剩余边界

实施范围限于适配器及其本地测试，没有恢复被停止的 GitHub/Sheet 生成，没有改冻结 evidence、线上产物或正式测试。

- **测试可信度**：`scenario_review.py` 把取消退出按自身分支校验；加入 `reload`、`sign_in`、`expect_sign_in_rejected` 与隔离的未知邮箱占位值。缺少角色绑定 fixture 的 team maintainer GIVEN 不再产生匿名/Owner 的机械 reach 测试，模型提议也须 skip 或提供有原文的争议。需求 THEN 中可明确提取的新账号登录、未知邮箱恢复/未建号、取消退出、退出后刷新分别计语义结果；每项要在同一个独立测试 case 内找到动作及断言，单纯入口/验证码不能冒充整段业务覆盖。覆盖 JSON 同时记源码 witness 和本次运行状态；漏项保持未验证。该提取器故意只认少量有明确原文依据的模式，未识别的其他结果**不能据此推断已覆盖**。
- **审核和隔离**：首审 schema 与证据化裁决对齐，只给目标失败完整证据；缺字段做一次同证据格式修正。无依据的“坏测试”意见或无效 JSON 不会隔离真实失败；有依据且二次只读审核一致的动作错误先尝试保留原断言的原子修正并重测，无法安全修正才精确隔离并留下覆盖缺口。审核不一致及机械 reach 搜索耗尽进入 unresolved，不驱动应用修复，也不晋升功能。原有 public/official spec 不参与此隔离。
- **helper 和编排**：SPA 点击时验证真实 URL 转移，表单等待精确标签并检查填值；新增负向登录验证 helper。统一顺序、波次与 repair 的依赖入口；回滚后重测并同步外部 test state，未重测的既有通过记录标为 unverified。执行代理拒绝本轮未提供的工具调用，包括旧式 `function_call` 和 `tool_choice=none`，保留允许的调用及 provider usage。
- **静态合同与预算**：相对/无路径 index 路由解析补根路径；发现禁止导航的 Link、bundled collection 的不存在方法时给出有边界的告警。代码未把这些正则提示当成硬性真实性裁决。结构化修复、协议重试和工具 fallback 共用 12 请求轮级上限；真实写入可在轮内走 8→12，部分写入被拒的分支最多 16，仍需受时间/token 门禁约束。

复审特别修正两处新引入风险。第一，单次审核模型缺失引用不代表测试错误：曾将它标 unresolved 并阻止应用修复，现改为保留原失败、仅记录审核不可用。第二，两次审核同意动作错误时，曾先隔离而跳过可保留 oracle 的修正，现先修正并重测，修正不可安全应用才隔离。SPA 等待也不再要求不同 URL 必须产生不同 `<main>` HTML，避免合法的同内容路由误判。

仍未完成的方案项有：通用状态指纹及所有共享文件变更后的受影响闭包重测、同分局部 API 修复的候选提交规则、集合单一 owner 的强制治理、所有产品语义分支的自动提取与跨会话验证。它们依赖更宽的架构改造和真实应用合同，不能用当前静态启发式安全替代。对这些项，本轮保留未验证状态与告警，不宣称已经解决。副作用主要是部分节点会因新增语义缺口停在未验证、审核可能多一次格式请求、精确标签与负向登录 helper 可能暴露旧 spec 的定位问题；控制办法是限定提取词汇、原文引用、可追溯账本和浏览器正反回归。下一次付费全题对照应在这些确定性修改冻结后单独进行；停止指令下不会自动启动。

验证记录：`PYTHONPATH=arc ARC_TEST_NPM_INTEGRATION=1 npm_config_offline=true OCTOS_TEST_PLAYWRIGHT_ROOT=/home/chris/Works/octos-arc/arc/local-grader python3 -m unittest discover -s arc/tests -q` 全量 **1656/1656 通过**；两项新 helper 场景使用真实 Chromium/Playwright 跑通延迟 SPA 注册字段和恢复页到负向登录的跳转。`python3 -m py_compile` 与 `git diff --check` 通过。首次在线 npm 安装遇到 120 秒超时；离线缓存下单项及全量 npm 集成均通过，因此不能将首次超时归因于应用逻辑。仍未运行完整付费 GitHub→Sheet 生成，故没有新的真实任务完成率或官方分数。
