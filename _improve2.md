# v11.1 下一轮：主任务优先的错误分级与非阻断测试流程

状态：2026-09-26 已完成一轮代码实施与本地回归；未重启已停止的付费整题任务。本文取代 `_improve.md` 和本轮诊断报告中“必须先让依赖测试完整通过才能构建下游”的调度建议；保留其中测试证据、错误测试隔离和不虚报通过的要求。下列任务框是原方案清单，实施结果以本节为准。

实施复审：生成状态和测试状态已分轴记录；依赖测试失败不再挡住下游源码尝试；F0/F1 有界处理，F2/T 转为只读自审；派生测试在应用行为测量前逐 case 建立版本化独立审核，未审核 case 不作为产品通过或应用修复依据；第一层分类成为接口合同及集成复核单位，wave 仍按小批 leaf 生成。无条件派生全集已取消，改为限额的可信文件抽测、最终 build/start 检查。`pack.sh` 已纳入新模块。

复审后保留的边界：静态语义回归已覆盖退出、未知邮箱等高风险强弱断言，但尚未对真实错误实现执行隔离变异样例；分类接口 owner 由全局设计和确定性归属推导，对设计遗漏仍只报 warning；未做新一轮 GitHub→Sheet 付费整题 A/B，因此成本和实际得分改进仍需冻结包实测。所有未审核、skip 和自审结果保持 `unverified`，不能据此称官方测试通过。

## 一、目标与已知证据

主目标是完成整题的源码生成、可运行应用和诚实交付。**任何派生测试失败、缺失、争议或超时都不得停止后续节点的代码生成，也不得要求 100% 测试通过才交付。** 运行时构建/启动崩溃属于必须优先处理的产品阻断；经过有界恢复仍无法启动时，可以保存源码和诊断，但交付必须明确标为不可运行，不能声称任务完成。

现有系统已对部分问题分流：`generation_checks.py` 有 source errors/warnings，`test_policy.py` 有 `valid/invalid/unresolved`，`RunSummary` 有失败、超时、基础设施未知；但 `main.py` 的节点裁决主要还是 `test_verdict: True/False/None`，`pending_dependencies()` 对既有外部依赖要求 `True`，`acceptance_loop()` 对失败可做多轮修复，`final_acceptance_passes()` 默认尝试全套。它没有统一的**产品影响等级 × 证据可信度 × 修复时机**，也没有把“生成进度”和“测试结果”作为两条独立状态轴。局部 warnings 或争议账本不等于全流程分级。

本轮 GitHub 证明现状有代价：已标 `unresolved` 的三层 reach 失败仍进入修复 prompt，导致无需求依据的登录菜单改动；只有 13/87 场景标记 covered、4/5 已识别语义合同缺失；缺合同又令 43/47 节点等待。随后 45 个内部 spec、88 个检查跑约 2,464 秒，28/88 的内部结果主要反映未构建和低信号测试，不能当官方分数。Sheet 本轮未启动，因此不能把 GitHub 数值外推成 Sheet 效果。

## 二、错误分级：按产品影响和证据，不按失败数量

| 等级 | 判定依据与例子 | 即时动作 | 未解决时的状态 |
| --- | --- | --- | --- |
| F0 可运行性/数据安全阻断 | 当前源码无法编译、服务反复崩溃、核心初始化无法完成，或有可复现的跨用户越权、不可逆数据破坏；必须有构建日志、异常或实际请求证据 | 保存当前候选，做有界的定向修复与最小 build/start/smoke；优先恢复最近可启动快照 | 继续尝试生成其他独立模块或离线代码，但最终标 `unusable/critical_open`，不得声称可运行或安全；不在单个错误上无限循环 |
| F1 核心业务缺陷 | 权威需求的主路径、正常身份/权限操作、持久化或跨模块必需接口确实失败，但尚无 F0 的崩溃、越权或不可逆破坏证据；用可靠行为测试、实际 API、或源码与需求的直接矛盾定位 | 有界定向修复一次，必要时再做一次独立复测；同时保持后续节点生成队列活跃 | `implemented_unverified/core_risk_open`，后续生成继续；交付显著列出风险，不能以测试跳过换成通过 |
| F2 局部业务缺陷 | 范围局限的字段、边缘分支、文案或非关键功能行为；权威要求可追溯，但不影响主路径/其他模块 | 记入待修账本，优先做一次源码自审；有空余预算且修复不影响主任务时再处理 | `deferred`，应用继续生成与交付；最终报告保留影响和证据 |
| T 低可信度/低执行价值测试 | 只有 reach/entry、搜索深度耗尽、helper 定位/时序可疑、前置不成立、oracle 与需求争议、污染状态、重复超时，或成本高而新增信息少 | **不据此修应用**；跳过本轮复跑/修复并做一次有边界的源码自审，记录测试原因和未测合同 | `test_skipped_or_disputed` + `unverified`；既不算失败的产品证据，也不算通过 |
| I 基础设施未知 | provider、Docker、测试 runner、网络/环境故障；不能归因于产品 | 有界诊断/恢复；不中途改应用来追逐环境错误 | `measurement_unavailable`，继续可独立完成的生成，不伪造任何产品裁决 |

分级必须是二维的：**严重度是需求和实际产品影响，可信度是观察来源和可复现性**。单个可靠的越权测试可升 F0/F1；100 个相同的 helper 超时仍是 T/I。仅由 AI 生成测试给出的失败不得自动判 F0/F1；须有独立的需求引用与运行/源码证据。反过来，没有测试覆盖也不能把已明显崩溃的应用降级。

若低可信度测试**声称**发现 F0（例如越权或删除数据），不能直接跳过，也不能直接改应用：先做一次受限的独立 API/构建复现或源码安全检查；证实后升 F0，无法证实则记 `critical_suspected_unverified` 并继续生成，最终报告显著列出。这保留对严重风险的敏感性，同时避免弱 oracle 控制调度。

“低执行度”在此落为可审计条件：测试只验证入口而不执行业务动作、未满足 GIVEN/WHEN、helper 用有界图搜索却未到目标、用例每次超时但未产生新的网络/DOM/异常证据、或与已有失败的证据签名相同。**不能只因运行时间长就跳过可靠的核心安全/数据合同。**

## 三、任务列表（按问题域列出；实施顺序见第五节）

### P0-1：建立两条独立状态轴与分类账本

- [ ] 增加节点生成状态 `not_started / source_written / buildable / attempted_with_risk / unusable`，与测试状态 `not_run / passed / failed / disputed / skipped_low_signal / measurement_unavailable` 分离。不要再用 `test_verdict=True` 判断节点是否可被生成。
- [ ] 每条失败记录 case ID、需求版本、spec/helper/fixture/source hash、观察层（构建、浏览器、API、源码）、证据链接、严重度、可信度、当轮动作、后续风险及负责节点。源/测试版本变化使旧判定 stale，而不是沿用旧 pass 或旧跳过。
- [ ] 分类器先用确定性信号：构建/启动日志、具体 HTTP/异常、测试是否真正走到业务动作、`test_policy` 判定、依赖能力、已知成本。模型可以提出分级建议，但 F0/F1 升级或测试隔离不能只靠一个模型自述；证据不足归 `unverified`。所有分类有上限与默认继续生成路径。
- [ ] 分别汇报节点已尝试数、源码已写数、当前可启动性、可靠合同通过数、待修风险、低信号跳过数。保留原始需求/测试总数作分母，另列实际执行数、跳过数和争议数；`skip` 绝不计为 pass，也不能通过缩小分母制造“高通过率”。

验收：同一组失败包含真实 `type=email` 表单错误和已登录 reach 搜索耗尽时，前者按需求影响进入 F1/F2，后者进入 T；分类与记录可以复现，用户能看出哪条实际修复、哪条仅自审。旧 `test_verdict` 仍可作为测试投影，不能再决定调度。

### P0-2：调度器保证所有节点都会被尝试生成

- [ ] 改 `pending_dependencies()` / `admit_node()` / whole-app wave fallback：测试结果、缺失 oracle、争议、低信号跳过都**不能**把下游节点标 `implementation_waiting` 并结束该节点。声明依赖只用于安排顺序、提供接口上下文和最终风险传播。
- [ ] 依赖源码尚未形成时，先生成明确的接口/数据合同或在同批构建；若运行能力确实未就绪，仍尝试写下游代码，保留 `blocked_at_runtime`，不伪造功能完成。独立节点不受其他模块失败影响。
- [ ] F0 有短暂优先恢复窗口；若仍失败，恢复最近可启动源码作为生成基线，并继续后续可写模块。没有可启动基线时进入**有总预算上限**的离线生成模式，构建/测试暂缓；最终如仍不可启动，明确失败退出而非无限等待或假完成。可写代码与可运行交付必须区分。
- [ ] 让 wave、顺序、repair、恢复和部分生成路径共享同一“下一节点必须尝试”的队列。每个未尝试节点必须有非测试原因（例如用户中断、硬预算耗尽或 provider 完全不可用）及证据；测试失败不在允许原因集合。

验收：以本轮 47 节点依赖链做无付费调度回放，注册 `5/5` 但语义合同缺失时仍尝试登录、恢复及其他下游；机械 `1/1` 仓库入口不能让仓库链等待。真实崩溃触发优先恢复，但失败后队列仍向前；最终可运行性不被假报。此项直接解决 43/47 停滞。

### P0-3：把测试失败从“强制修到绿”改为有界处置

- [ ] 从所有应用修复入口生成**唯一 repair target 清单**。`T/disputed/skipped/stale` 用例不得出现在“待修失败”摘要、可编辑 spec 摘录或工具修复目标中；模型只看到单独的排除账本。执行前检查目标 case ID 与 prompt，执行后审核新增 UI/路由/权限行为是否有独立需求依据。
- [ ] F0 运行必要 build/start/最小 smoke，允许有界定向修复；F1 只给少量针对性修复及可靠复测；F2 默认自审并延期；T **首次失败即可停止复跑，不做应用修复**，改做一次源码自审。重复完全相同证据不再扩大预算或触发 full rewrite。
- [ ] 自审只读权威需求、有关源码和已有可靠观测，回答“实现了什么、明确缺什么、有哪些高风险疑点”，标注源码路径和需求原句；不能从代码存在推断运行成功。若发现直接导致 F0/F1 的确定性错误，可升级并做定向修复；否则留下 `reviewed_unverified`，继续下一个节点。多个低信号用例可按同一模块批量自审，限制模型调用。
- [ ] 特别复放本轮 `proxy-00008/00009`：有效注册表单修复可以保留；`unresolved` 的 logged-in reach 不得进入 repair prompt，也不得驱动账户菜单新增 `Sign in`。与 `_improve.md` 一致，错误测试原文和判定证据保留，不改 public/official 测试。

验收：低信号用例失败后零次为该用例而发的应用写入，最多一次有界自审，节点仍前进；F0/F1 有可靠证据时仍会被处理。自审失败/超时也不能阻断队列，不会变成绿色测试结果。

### P0-4：测试生成与执行改为抽样、按价值准入

- [ ] 修复 wave gate 失败后绕过 `prepare_derived_spec_batch()` 的路径，但把 spec 准备改成**尽力而为、硬超时、限额、可延后**：优先身份、安全、持久化及主路径；模型超时或校验失败立即记录缺口并继续生成，其余场景缺测试时也不阻断，不把入口 `2/2` 晋升为功能完成。
- [ ] 保持生成产物按 leaf 存放、覆盖按 scenario/合同判定、执行按 case 隔离。第一层 folder 可作为**实现阶段与集成复核单位**，但不能当成一个大测试单元或一次巨型代码生成请求；跨 leaf 的模型批处理不得混淆 case 所属需求和状态。
- [ ] 每个节点做廉价静态自审，构建按波次/共享文件变化或 F0 疑点触发，避免 47 次重复构建；仅对高风险或确实已构建的能力做针对性行为测试。低执行度用例一旦失败就停止测试追逐，进入自审；无新证据的超时/重复失败不进入回归 backlog。
- [ ] `final_acceptance_passes()` 不以“有失败就继续全套”为策略。已构建且可靠的核心合同和受共享源码影响的既有行为优先；未构建节点列 `not_run`。全套最多一次、须有预计耗时与剩余预算准入；估算用实际每 spec 用例数、导航/状态隔离耗时，而非只用文件数和单用例 timeout。
- [ ] 显式分配**可调**的时间/token 配额给生成、F0 恢复、F1 验证、自审和最终 smoke。初始实验档可把低信号测试的复跑/修复限制为零、把其自审与普通 F2 测试合计限制在约 10–20% 墙钟；F0/F1 和最终 build/start/smoke 单独保留预算。比例须用冻结记录校准，并非已证实最优值。

验收：对本轮 45 spec 的历史时长回放，低信号/未构建节点不会默认耗约 2,464 秒；关键构建和核心行为仍被测。生成节点数和可启动性优先提升，测试覆盖缺口被清楚报告而非隐藏。

### P0-5：每个新测试生成后独立复审一次，再决定能否作为修复证据

现行实际粒度是：`compile_suite()` 遍历所有 leaf，`compile_leaf()` 逐个处理其中的 scenario，并把可生成的检查写为 `REQ-...spec.ts`（一 leaf 一文件；无检查的 leaf 暂无文件）。`review_targets(..., include_all=True)` 为每个去重后的 scenario 建目标；`augment_derived_tests()` 默认将 6 个 scenario 合成一次 AI 请求，优先让每个 leaf 获得首个目标，**一个请求可跨 leaf**；单个 scenario 提案可产生 1–8 个独立 case。编译器现在会校验提案 DSL/字面量，非法提案可重试一次，`verify_derived_suite()` 检查 Playwright 能否加载；但加载成功不证明 GIVEN/WHEN/THEN、身份、状态隔离及 oracle 正确。失败后审核也不能替代**生成完成、应用代码尚未影响意见时**的独立复审。模型给出的 `skip` 当前可直接结束该 scenario 的提案，尤其需要复核。

- [ ] 为机械和 AI 生成的**每个 case**及每个 `skip` 建独立 review 记录，键为 leaf ID + scenario ID + case ID + 需求/spec/helper/fixture 哈希；文件加载检查仍按 leaf 做。记录 reviewer 所见的完整权威 GIVEN/WHEN/THEN、祖先约束和角色 fixture，跨 leaf 批处理不得共享错误的前置或 oracle。
- [ ] 每个新 case 入库前做一次**独立语义复审**：审查者只看需求、fixture/helper 和生成的 spec，不看当前应用源码或测试通过率；逐项核对演员/身份、前置状态、动作顺序、正反分支、重载/重新登录、持久化、断言是否真正验证 THEN、测试间 reset 和字面量依据。先运行独立的确定性结构审计与 Playwright 加载；复杂语义再用与生成请求分开的只读模型复审，允许按 leaf/风险批量化以控制成本。不能拿原生成器的“我已检查”当第二次审核。
- [ ] 审核给出 `approved_behavior / approved_smoke_only / needs_correction / disputed / skipped_with_reason / unreviewed_timeout`，并附需求原句、对应动作/断言及缺口。只有 `approved_behavior` 且运行证据可靠的 case 才可驱动应用修复；入口/reach 即使通过也只能作 smoke。`skip` 必须说明为何原场景不可验证并被单独复核，不能自动抹掉需求合同。
- [ ] 对身份、退出、权限、数据持久化等高风险 oracle，使用少量**隔离的错误实现样例**做反向灵敏度检查：例如取消退出后会话实际失效但仍隐藏 `Sign in` 时，测试必须失败；未知邮箱错误创建账号时，测试必须失败。只检验测试能否区分关键对错，不修改正在生成的应用，也不对所有普通用例做昂贵的变异测试。
- [ ] 审核不通过时最多一次**针对该 case**的修正及复审；仍有争议或超时就把它降为低信号/未验证，进入代码自审并继续生成，不扩大修测试请求或等待整批全过。`derived_augmented_nodes` 当前在模型请求前标已尝试，需拆为 attempted/accepted/reviewed；失败批次可在版本不变、预算允许时有界重试，不能误记为已审核。
- [ ] 保存原测试、审核意见和更正版本，不静默删强断言。`covered`、`runtime_passed`、`review_approved` 三项分开报告；审核结论随任一相关哈希变化而失效。官方/public spec 保持只读，不参与此派生测试编辑策略。

验收回放至少包含：Cancel 后会话保持被通用回显规则误拒、重试后弱化 oracle、恢复场景漏掉先登录、注册的错误 signed-in reach、未知邮箱未证明“不建账号”、以及一个确实正确的强断言。复审应保留正确强断言，识别前置与负向缺口；低信号/争议用例不进入应用 repair，真实 F0/F1 仍可由独立证据升级。审核服务不可用时所有节点继续生成，但未审核测试不得变成修复或通过依据。

### P0-6：以第一层大分类组织阶段，以小批 leaf 实施并跨阶段集成

逐 leaf 独立实现会丢失相关功能的共同状态模型、路由、权限和数据 owner；但“整个第一层分类一次生成”也不合适。实际 GitHub 有 6 个第一层分类，各含 5–12 个 leaf、14–25 个原始 scenario；Sheet 有 5 类，各含 4–6 个 leaf、13–26 个原始 scenario。当前已有一次全树 `app_design()`，`whole_app_waves()` 默认最多 6 leaf/请求且按上下文和输出预算缩批；这些机制提供了部分全局相关性，但没有可靠的**分类阶段集成检查**。Sheet 需求树还存在 REQ-2 与 REQ-5 的双向跨类依赖，因此绝不能“先完整验收第 2 类才开始第 5 类”，也不能以分类测试全绿当下一阶段门槛。

- [ ] 在全树设计之后，为每个第一层分类从需求树与全树设计**抽取**简短、版本化的阶段合同：该类 leaf ID、共享实体/字段和 owner、API/页面、身份/权限、状态生命周期、与其他分类的输入输出，以及不可破坏的已实现合同。仅在接口含糊时做有界模型补全，避免每类都增加一次强制 LLM 请求。用确定性规则检查 leaf ID 覆盖、重复 route/collection owner、缺失跨类接口；无有效阶段合同时退回全树设计与原需求，不停生成。
- [ ] 以分类为计划、预算、进度和集成复核单位；以**相邻、依赖允许且预算可容纳的 leaf 小批**为代码生成单位。单类超过请求/输出预算就拆批，同类 leaf 仍共享同一阶段合同与源码 owner；跨类共享的 auth、store、router 按全树设计指定一个 owner，不能每类另建一套。
- [ ] 构造分类间**能力依赖图**而非测试通过图；对跨类环先共同约定接口/数据形状，再交替实现相关 leaf。分类阶段状态仅表示 `planned / partially_written / integrated_unverified / runnable`，不要求该类测试 100% 通过，也不阻断其他类开始。分类间功能可回访或并行推进，硬预算耗尽除外。
- [ ] 每批代码提交后做轻量的共享文件/接口一致性检查；在一个分类主要 leaf 写完时，做一次跨 leaf 自审和少量代表性端到端 smoke（例如注册→登录→仓库权限、工作簿→工作表→单元格→公式），只检查已构建且有可靠 oracle 的路径。发现 F0/F1 有界修复，F2/T 记账继续；大类阶段完成不是全套测试门槛。
- [ ] 把阶段合同、leaf 代码、逐 scenario 测试和实际结果分开存证；共享接口/源码改版时，相关阶段集成证据变 stale 并只重测受影响路径。复审模型不得以“该类设计完毕”声称所有 leaf 已实现。

验收：用 GitHub 6 类和 Sheet 5 类的冻结需求树重放分组，所有 leaf 都归入唯一阶段，跨类接口 owner 唯一；GitHub 大类 12 leaf 不产生一次超预算巨型请求，Sheet REQ-2↔REQ-5 的循环不造成调度死锁。类别自审应发现跨 leaf 的身份/数据接口矛盾；某一类的争议或失败测试不能阻止下一类代码生成。

复审副作用：阶段规划增加抽取/校验成本，少数含糊接口可能多耗模型请求；阶段合同可能过时，跨类共享 owner 可能成为新热点文件。通过从已有全树设计复用信息、短合同、版本/hash 失效、按小批拆分、共享模块抽取和受影响闭包复核控制；不引入“第一层全绿才推进”的新 gate。该方案增强功能相关性，但仍不能单凭阶段规划证明真实行为正确。

### P1：提示词、预算和最终反馈与新策略一致

- [ ] 生成提示词明确“实现权威需求并完成全树，测试是观测而非唯一目标”；模型不得为低可信度测试加无需求 UI、删 oracle 或宣称未运行行为已验证。修复提示词按 F0/F1/F2/T 的结构化分类输入，输出已观察/源码推断/未验证/下一步。
- [ ] 单次修复轮、整节点和整题共用请求/time/token 上限。只在已写源码或新增可靠证据时增配，不再为同一失败反复 8+12 请求。F0 预算可以高于 F1/F2，但必须有终止与恢复策略；不简单增大 promote 预算。
- [ ] 最终反馈按“可启动性、已生成能力、已验证核心合同、未验证/跳过/争议、已知 F0/F1 风险”列出；报告内部测试数仅作诊断，绝不称官方成绩。生成可以完成但质量仍不确定，只有通过 build/start/smoke 才能标“可运行”。

## 四、方案复审：副作用、边界与修正

| 风险 | 为什么真实存在 | 控制措施 |
| --- | --- | --- |
| 跳过测试掩盖真实缺陷 | AI 用例虽弱，背后可能确有产品问题 | 低信号不等于合格：权威需求逐条自审、F0 build/start 和高风险最小 smoke 保留；跳过单列且可重新激活 |
| 自审产生幻觉 | 看到代码不代表真实操作成功 | 自审报告必须引用源码与需求，状态只到 `reviewed_unverified`；F0/F1 升级要运行/异常或确定性矛盾证据 |
| 放行依赖后扩大返工 | 未完成上游可能让下游接口假设错误 | 先冻结接口/数据合同、共享源码改动做受影响闭包检查，版本变化使下游证据 stale；允许代码生成但不宣称功能验证 |
| 真实崩溃也继续推进导致垃圾产物 | 无法启动的基线可能污染整题 | 短时优先恢复、最近可启动快照、离线候选隔离；最后无法 build/start 时明确 `unusable`，不以“节点已尝试”冒充完成 |
| 少跑全套漏跨模块回归 | 共享路由/身份状态容易互相影响 | 对已可靠验证的主路径做变更影响复测；最终 build/start + 核心 smoke；全套有预算和实现覆盖时再运行 |
| 错误测试仍通过上下文诱导编辑 | 本轮已发生 `unresolved` 泄漏 | repair target 与提示词/候选差分统一校验；混合修复按 hunk 拆分，不能仅靠模型一句“忽略该测试” |
| 每例独立复审增加成本且可能误拒 | 87 个 scenario、一个 scenario 最多 8 case，逐例单独请求会吞掉生成预算 | 每 case 必有审计记录，确定性检查覆盖全部；语义审核可在同 leaf 或小批量只读请求中逐 case 给出结论，硬限时，未完成标未审核并继续生成；正确强断言有正向回归 |
| 改成非阻断后状态含糊 | “完成生成”易被误读为“满足全部需求” | 生成进度与产品验证分轴；未验证、延期、争议、不可运行均是显式终态；所有百分比写清分母 |

复审结论：**可行**，但不能把“测试不阻断”简化成“测试失败都忽略”。正确边界是测试永不控制节点生成队列；有独立证据的崩溃/安全/核心能力问题得到有界优先修复；低可信度、低执行价值用例跳过本轮追逐并做源码自审；最终风险透明。这样与“不需 100% 通过”和“程序崩溃必修”同时相容。若程序最终仍不可运行，流程可终止并交付诊断/源码，但不能称主任务成功，这是不可取消的真实性约束。

## 五、验证顺序和停损条件

1. 先用保存的 GitHub 交互、测试状态和 45 个 spec 时长做无付费回放，写上述分级、调度、修复目标、预算的正反向回归；同时加入“可靠安全失败一例仍须升级”和“真实构建崩溃不得误跳过”。
2. 实施 P0-1/P0-2 后验证 47 节点都进入生成尝试；在全树设计与小批生成之间加入 P0-6 的阶段合同，并用 GitHub 12-leaf 大类与 Sheet 跨类环验证不会形成巨型请求或阶段死锁。实施 P0-4/P0-5 的生成与复审后，再以 P0-3 验证 `unresolved`/未审核用例不进入修复 prompt、混合真实失败仍能修；最后验证总测试成本不再吞掉生成时段。每一步运行现有单元、构建和必要的本地浏览器/API 集成，再独立代码复审。
3. 先在冻结的单一失败输入上有限重放提示词/模型效果；它不能证明整题收益。待硬门禁稳定、预算可预测，再另行决定 GitHub→Sheet 的完整付费对照；不自动恢复已停止的本地任务。

停损条件：测试 skip/未审核被记成 pass、F0 崩溃被标可运行、由争议测试再次诱发应用写入、任一测试失败使后续节点未被尝试、或削减测试后核心身份/数据 smoke 被跳过。出现任一项即回退对应策略，不通过增大预算掩盖问题。
